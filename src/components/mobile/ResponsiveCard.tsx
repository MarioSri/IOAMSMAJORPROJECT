import React from 'react';
import { ResponsiveCard as UIResponsiveCard } from '@/components/ui/responsive-card';

/**
 * ResponsiveCard component for mobile-optimized layouts.
 * Re-exports the UI implementation for consistency across the mobile dashboard.
 */
export const ResponsiveCard = UIResponsiveCard;

export default ResponsiveCard;